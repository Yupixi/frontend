Command finished with exit code 0.
Command output:
.hero-premium {
  background: var(--bg);
  border-bottom: 1px solid var(--border);
  position: relative;
  overflow: hidden;
}
.dark .hero-premium {
  background: #09090B;
  border-bottom: 1px solid #18181B;
}

.hero-premium-glow {
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 0;
}

.hero-glow-1 {
  position: absolute;
  top: -10%;
  right: -10%;
  width: 60%;
  height: 60%;
  background: radial-gradient(circle, rgba(254,0,0,0.08) 0%, transparent 70%);
  filter: blur(80px);
  animation: float-glow 20s infinite alternate ease-in-out;
}

.hero-glow-2 {
  position: absolute;
  bottom: -10%;
  left: -10%;
  width: 40%;
  height: 40%;
  background: radial-gradient(circle, rgba(255,221,33,0.05) 0%, transparent 70%);
  filter: blur(60px);
  animation: float-glow 15s infinite alternate-reverse ease-in-out;
}

@keyframes float-glow {
  0% { transform: translate(0, 0) scale(1); }
  100% { transform: translate(-5%, 5%) scale(1.1); }
}

.hero-premium-inner {
  max-width: 1280px;
  margin: 0 auto;
  padding: 6rem 1rem 7rem;
  position: relative;
  z-index: 1;
}

.hero-grid {
  display: grid;
  grid-template-columns: 1.1fr 0.9fr;
  gap: 4rem;
  align-items: center;
}

@media (max-width: 1024px) {
  .hero-grid {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 3rem;
  }
  .hero-premium-inner {
    padding: 4rem 1rem;
  }
}

@media (max-width: 768px) {
  .hero-premium-inner {
    padding: 2.5rem 1rem 3rem !important;
  }
  .hero-title {
    font-size: 2.2rem !important;
    line-height: 1.1 !important;
  }
  .hero-subtitle {
    font-size: 1rem !important;
    margin-bottom: 0.5rem !important;
  }
  .hero-badge {
    padding: 6px 12px !important;
    font-size: 0.75rem !important;
  }
  .hero-search-wrapper {
    flex-direction: column !important;
    gap: 0.75rem !important;
    padding: 0.5rem !important;
    background: transparent !important;
    border: none !important;
    box-shadow: none !important;
  }
  .hero-search-input-box {
    background: var(--bg-card) !important;
    border: 1px solid var(--border) !important;
    border-radius: 12px !important;
    padding: 0.75rem 1rem !important;
    width: 100% !important;
  }
  .hero-search-btn {
    width: 100% !important;
    padding: 0.875rem !important;
    border-radius: 12px !important;
  }
}

/* --- Left Column --- */
.hero-left {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

@media (max-width: 1024px) {
  .hero-left {
    align-items: center;
  }
}

.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(254,0,0,0.05);
  border: 1px solid rgba(254,0,0,0.1);
  padding: 8px 16px;
  border-radius: 999px;
  font-family: 'Outfit', sans-serif;
  font-weight: 700;
  font-size: 0.85rem;
  color: var(--primary);
  width: fit-content;
  backdrop-filter: blur(8px);
}

.hero-title {
  font-family: 'Outfit', sans-serif;
  font-weight: 900;
  font-size: clamp(2.5rem, 5vw, 4rem);
  line-height: 1.05;
  color: var(--fg);
  margin: 0;
  letter-spacing: -0.04em;
}

.hero-title-accent {
  position: relative;
  display: inline-block;
  color: var(--primary);
}

.hero-title-accent::after {
  content: '';
  position: absolute;
  bottom: 8px;
  left: 0;
  width: 100%;
  height: 8px;
  background: rgba(254,0,0,0.1);
  z-index: -1;
  border-radius: 4px;
}

.hero-subtitle {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1.15rem;
  line-height: 1.6;
  color: var(--fg-muted);
  margin: 0;
  max-width: 540px;
}

/* --- Search Bar Hero --- */
.hero-search-container {
  width: 100%;
  max-width: 600px;
  margin-top: 1rem;
}

.hero-search-wrapper {
  display: flex;
  align-items: center;
  background: var(--bg-card);
  border: 1px solid var(--border);
  padding: 8px;
  border-radius: 20px;
  box-shadow: 0 10px 30px -10px rgba(0,0,0,0.1);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.hero-search-wrapper:focus-within {
  border-color: var(--primary);
  box-shadow: 0 15px 40px -12px rgba(254,0,0,0.15);
  transform: translateY(-2px);
}

.hero-search-input-box {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 0 16px;
}

.hero-search-input {
  width: 100%;
  border: none;
  outline: none;
  background: transparent;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1rem;
  font-weight: 500;
  color: var(--fg);
}

.hero-search-btn {
  background: var(--primary);
  color: white;
  border: none;
  border-radius: 14px;
  padding: 12px 24px;
  font-family: 'Outfit', sans-serif;
  font-weight: 800;
  font-size: 0.95rem;
  cursor: pointer;
  transition: all 0.2s;
  display: flex;
  align-items: center;
  gap: 8px;
}

.hero-search-btn:hover {
  background: var(--primary-dark);
  transform: scale(1.02);
}

@media (max-width: 640px) {
  .hero-search-wrapper {
    flex-direction: column;
    padding: 12px;
    border-radius: 24px;
    gap: 12px;
  }
  .hero-search-btn {
    width: 100%;
    justify-content: center;
  }
  .hero-search-input-box {
    padding: 8px 0;
    width: 100%;
  }
}

.hero-stats {
  display: flex;
  gap: 3rem;
  margin-top: 1.5rem;
}

@media (max-width: 1024px) {
  .hero-stats {
    justify-content: center;
  }
}

@media (max-width: 640px) {
  .hero-stats {
    gap: 1.5rem;
    flex-wrap: wrap;
    justify-content: center;
  }
}

.hero-stat-item {
  display: flex;
  flex-direction: column;
}

.hero-stat-value {
  font-family: 'Outfit', sans-serif;
  font-weight: 900;
  font-size: 1.75rem;
  color: var(--fg);
  line-height: 1;
}

.hero-stat-label {
  font-size: 0.85rem;
  color: var(--fg-muted);
  font-weight: 600;
  margin-top: 4px;
}

/* --- Right Column: Floating Mosaic --- */
.hero-right {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 500px;
}

.hero-floating-container {
  position: relative;
  width: 100%;
  height: 100%;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
  perspective: 1000px;
}

.hero-mosaic-card {
  position: relative;
  background: var(--bg-card);
  border-radius: 24px;
  overflow: hidden;
  border: 1px solid var(--border);
  box-shadow: 0 10px 25px -5px rgba(0,0,0,0.05);
  transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  cursor: pointer;
}

.hero-mosaic-card:hover {
  transform: translateY(-10px) rotateX(2deg) rotateY(-2deg) scale(1.02);
  box-shadow: 0 20px 40px -10px rgba(0,0,0,0.12);
  z-index: 10;
  border-color: rgba(254,0,0,0.2);
}

.hero-mosaic-card-main {
  grid-row: span 2;
  height: 100%;
}

.hero-mosaic-img-wrap {
  width: 100%;
  height: 100%;
  position: relative;
}

.hero-mosaic-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
}

.hero-mosaic-card:hover .hero-mosaic-img {
  transform: scale(1.1);
}

.hero-mosaic-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to top, rgba(15, 23, 42, 0.8) 0%, rgba(15, 23, 42, 0.2) 50%, transparent 100%);
}

.hero-mosaic-content {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 20px;
  color: white;
}

.hero-mosaic-category {
  font-size: 0.7rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--secondary);
  margin-bottom: 4px;
}

.hero-mosaic-title {
  font-size: 1rem;
  font-weight: 800;
  line-height: 1.2;
  margin: 0 0 8px;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.hero-mosaic-price {
  font-family: 'Outfit', sans-serif;
  font-weight: 900;
  font-size: 1.25rem;
}

.hero-mosaic-price-lg {
  font-size: 1.5rem;
}

.hero-mosaic-fav {
  position: absolute;
  top: 16px;
  right: 16px;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: rgba(255,255,255,0.2);
  backdrop-filter: blur(8px);
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(255,255,255,0.3);
  color: white;
  transition: all 0.2s;
}

.hero-mosaic-fav:hover {
  background: white;
  color: var(--primary);
}

/* Animations for staggered entry */
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.hero-left > *, .hero-stat-item {
  animation: fadeInUp 0.8s cubic-bezier(0.22, 1, 0.36, 1) both;
}

.hero-mosaic-card {
  animation: fadeInUp 1s cubic-bezier(0.22, 1, 0.36, 1) both;
}
.hero-btn-outline {
  background: transparent;
  color: var(--fg);
  border: 1px solid var(--border);
}
.hero-btn-outline:hover {
  background: var(--border-subtle);
  border-color: var(--primary);
  color: var(--primary);
  transform: translateY(-1px);
}
.dark .hero-btn-outline {
  border-color: #3A2727;
  color: #E0D0D0;
}
.dark .hero-btn-outline:hover {
  border-color: var(--primary);
  color: var(--primary);
}

.hero-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.5rem;
  background: var(--border-subtle);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 1rem 1.25rem;
  transition: all 0.2s;
}
.dark .hero-stats {
  background: #1F1414;
  border-color: #3A2727;
}

.hero-stat-item {
  text-align: center;
}
.hero-stat-item + .hero-stat-item {
  border-left: 1px solid var(--border);
  padding-left: 1rem;
}
.dark .hero-stat-item + .hero-stat-item {
  border-color: #3A2727;
}

.hero-stat-value {
  font-family: 'Outfit', sans-serif;
  font-weight: 900;
  font-size: 1.2rem;
  color: var(--primary);
}
.hero-stat-label {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 0.75rem;
  color: var(--fg-muted);
  margin-top: 2px;
  font-weight: 600;
}

/* --- Right Column: Mosaic --- */
.hero-right {
  position: relative;
}

.hero-mosaic {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  grid-template-rows: 160px 160px;
  gap: 10px;
  animation: heroMosaicIn 0.8s ease-out backwards;
}

.hero-mosaic-card {
  position: relative;
  border-radius: var(--radius);
  overflow: hidden;
  cursor: pointer;
  background: var(--bg-card);
  border: 1px solid var(--border);
  transition: border-color 0.25s ease, transform 0.25s ease;
  display: flex;
  flex-direction: column;
}
.dark .hero-mosaic-card {
  background: #2A1B1B;
  border-color: #3A2727;
}
.hero-mosaic-card:hover {
  transform: translateY(-2px);
  border-color: var(--primary);
  z-index: 5;
}
.dark .hero-mosaic-card:hover {
  border-color: #5A3737;
}

.hero-mosaic-card-main {
  grid-column: 1 / 3;
  grid-row: 1 / 3;
}
/* Boosted cards placement (4 cards) */
.hero-mosaic-card:nth-child(2) { grid-column: 3; grid-row: 1; }
.hero-mosaic-card:nth-child(3) { grid-column: 4; grid-row: 1; }
.hero-mosaic-card:nth-child(4) { grid-column: 3 / 5; grid-row: 2; }
/* A 5th card only exists as the mobile carousel's trailing loop clone
   (see hero-mosaic-dots comment) �?" never shown in the desktop grid. */
.hero-mosaic-card:nth-child(5) { display: none; }

/* Anim delay per card */
.hero-mosaic-card:nth-child(1) { animation-delay: 0s; }
.hero-mosaic-card:nth-child(2) { animation-delay: 0.3s; }
.hero-mosaic-card:nth-child(3) { animation-delay: 0.6s; }
.hero-mosaic-card:nth-child(4) { animation-delay: 0.9s; }

.hero-mosaic-img-wrap {
  position: relative;
  width: 100%;
  flex: 1;
  min-height: 0;
  overflow: hidden;
}

.hero-mosaic-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.4s ease;
}
.hero-mosaic-card:hover .hero-mosaic-img {
  transform: scale(1.06);
}

.hero-mosaic-img-fallback {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--border-subtle);
  color: var(--fg-subtle);
}

.hero-mosaic-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, transparent 40%, rgba(0,0,0,0.65) 100%);
}

.hero-mosaic-boost {
  position: absolute;
  top: 8px;
  left: 8px;
  z-index: 3;
  background: #FFDD21;
  color: #0F172A;
  font-family: 'Outfit', sans-serif;
  font-weight: 900;
  font-size: 0.6rem;
  letter-spacing: 0.04em;
  padding: 3px 8px;
  border-radius: 6px;
  display: inline-flex;
  align-items: center;
  gap: 3px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.18);
}

.hero-mosaic-category {
  position: absolute;
  top: 32px;
  left: 8px;
  background: rgba(255,255,255,0.92);
  color: #0F172A;
  font-family: 'Outfit', sans-serif;
  font-weight: 800;
  font-size: 0.65rem;
  padding: 2px 8px;
  border-radius: 6px;
  backdrop-filter: blur(4px);
}
.dark .hero-mosaic-category {
  background: rgba(42,27,27,0.9);
  color: #E0D0D0;
}

.hero-mosaic-verified {
  position: absolute;
  top: 8px;
  right: 8px;
  color: #00A3E0;
  background: rgba(255,255,255,0.9);
  border-radius: 50%;
  width: 22px;
  height: 22px;
  display: flex;
  align-items: center;
  justify-content: center;
  backdrop-filter: blur(4px);
}
.dark .hero-mosaic-verified {
  background: rgba(42,27,27,0.9);
}

.hero-mosaic-price {
  position: absolute;
  bottom: 8px;
  left: 8px;
  font-family: 'Outfit', sans-serif;
  font-weight: 900;
  font-size: 0.8rem;
  color: #FFFFFF;
  text-shadow: 0 1px 6px rgba(0,0,0,0.4);
}
.hero-mosaic-price-lg {
  font-size: 1.1rem;
  bottom: 14px;
  left: 12px;
}

.hero-mosaic-fav {
  position: absolute;
  bottom: 8px;
  right: 8px;
  background: rgba(255,255,255,0.85);
  border: none;
  border-radius: 50%;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.2s;
  backdrop-filter: blur(4px);
}
.hero-mosaic-card-main .hero-mosaic-fav {
  bottom: 12px;
  right: 12px;
  width: 30px;
  height: 30px;
}
.hero-mosaic-fav:hover {
  transform: scale(1.15);
  background: #FFFFFF;
}
.dark .hero-mosaic-fav {
  background: rgba(42,27,27,0.85);
}
.dark .hero-mosaic-fav:hover {
  background: #4A3535;
}

.hero-mosaic-body {
  padding: 10px 12px 12px;
  background: var(--bg-card);
  transition: background 0.2s;
}
.dark .hero-mosaic-body {
  background: #2A1B1B;
}

.hero-mosaic-title {
  font-family: 'Outfit', sans-serif;
  font-weight: 800;
  font-size: 0.85rem;
  color: var(--fg);
  margin: 0 0 4px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.hero-mosaic-location {
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 0.72rem;
  color: var(--fg-muted);
  font-weight: 600;
}

/* Secondary cards get a more compact body than the featured one */
.hero-mosaic-card:not(.hero-mosaic-card-main) .hero-mosaic-body {
  padding: 8px 10px;
}
.hero-mosaic-card:not(.hero-mosaic-card-main) .hero-mosaic-title {
  font-size: 0.78rem;
  margin-bottom: 2px;
}
.hero-mosaic-card:not(.hero-mosaic-card-main) .hero-mosaic-location {
  display: none;
}

/* --- Espace Boost header --- */
.hero-boost-header {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 0.75rem;
}
.hero-boost-pill {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  background: #FFDD21;
  color: #0F172A;
  font-family: 'Outfit', sans-serif;
  font-weight: 900;
  font-size: 0.82rem;
  padding: 5px 13px;
  border-radius: 999px;
  white-space: nowrap;
}
.hero-boost-note {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 0.75rem;
  color: var(--fg-muted);
  font-weight: 600;
}
.hero-boost-hint {
  display: none;
  align-items: center;
  gap: 2px;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 0.72rem;
  color: var(--fg-muted);
  font-weight: 700;
  margin-left: auto;
}

/* Slide indicator dots �?" only meaningful for the mobile carousel below;
   the desktop mosaic is a static grid, not a scroller. */
.hero-mosaic-dots {
  display: none;
  justify-content: center;
  align-items: center;
  gap: 6px;
  margin-top: 12px;
}
.hero-mosaic-dot {
  padding: 0;
  border: none;
  cursor: pointer;
  width: 6px;
  height: 6px;
  border-radius: 999px;
  background: var(--border);
  transition: all 0.25s ease;
}
.dark .hero-mosaic-dot {
  background: #4A3535;
}
.hero-mosaic-dot.active {
  width: 20px;
  background: var(--primary);
}

/* --- Responsive --- */
@media (max-width: 1024px) {
  .hero-grid {
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }
  .hero-mosaic {
    max-width: 600px;
    margin: 0 auto;
  }
}

@media (max-width: 640px) {
  .hero-premium-inner {
    padding: 1.25rem 1rem 1.5rem;
  }
  .hero-grid {
    gap: 0;
  }
  /* Grid items default to min-width: auto, so .hero-mosaic's overflow-x:auto
     row (whose negative-margin bleed depends on a correctly-sized containing
     block) was letting its own scrollable content dictate .hero-right's
     width instead of shrinking to the actual grid track �?" inflating the
     whole carousel past the viewport. */
  .hero-right {
    min-width: 0;
  }
  /* The marketing block (badge/title/subtitle/CTAs/stats) is redundant on
     mobile �?" the header search bar and bottom nav already cover those
     actions �?" and pushes real content below the fold. Lead with "�? la
     une" instead, which is real listings, not landing-page copy. */
  .hero-left {
    display: none;
  }
  .hero-mosaic {
    display: flex;
    max-width: none;
    overflow-x: auto;
    gap: 12px;
    margin: 0 -1rem;
    padding: 4px 1rem 10px;
    scroll-snap-type: x mandatory;
    scroll-padding-left: 1rem;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    animation: none;
  }
  .hero-mosaic::-webkit-scrollbar { display: none; }
  .hero-mosaic-card:nth-child(5) { display: flex; }
  .hero-mosaic-card {
    grid-column: auto !important;
    grid-row: auto !important;
    flex: 0 0 72%;
    min-height: 0;
    height: auto;
    scroll-snap-align: start;
    animation: none;
    border: 1.5px solid var(--border);
  }
  .hero-mosaic-card-main {
    flex-basis: 72%;
  }
  .hero-mosaic-card:hover {
    transform: none;
  }
  .hero-mosaic-img-wrap {
    flex: none;
    width: 100%;
    aspect-ratio: 16 / 9;
  }
  .hero-mosaic-card .hero-mosaic-body {
    padding: 10px 12px 12px;
  }
  .hero-mosaic-card .hero-mosaic-location {
    display: flex;
  }
  .hero-mosaic-title {
    font-size: 0.85rem !important;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .hero-mosaic-price,
  .hero-mosaic-price-lg {
    bottom: 10px;
    left: 10px;
    font-size: 0.9rem;
  }
  .hero-mosaic-fav,
  .hero-mosaic-card-main .hero-mosaic-fav {
    bottom: auto;
    top: 8px;
    right: 8px;
  }
  .hero-mosaic-verified {
    top: 32px;
    right: 8px;
  }
  .hero-boost-pill {
    font-size: 0.88rem;
    padding: 6px 15px;
  }
  .hero-boost-note {
    display: none;
  }
  .hero-boost-hint {
    display: inline-flex;
  }
  .hero-mosaic-dots {
    display: flex;
  }
  .hero-stats {
    grid-template-columns: repeat(3, 1fr);
    gap: 0.25rem;
    padding: 0.75rem;
  }
  .hero-stat-value { font-size: 1rem; }
  .hero-stat-label { font-size: 0.65rem; }
  .hero-buttons { flex-direction: column; }
  .hero-btn { justify-content: center; width: 100%; }
  .hero-subtitle { font-size: 0.95rem; }
}